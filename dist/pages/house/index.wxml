
  <view class="container houseindex">
    <view class="index_list">
        <view bindtap="toDetail" class="index_item" data-url="/pages/house/detail?id={{item.id}}"  wx:for="{{page.list}}" wx:key="list*" data-wpytodetail-a="{{item.id}}">
            <image mode="aspectFill" src="{{item.thumb}}" class="post"/>
            <view class="title">{{item.title}}</view>
        </view>
    </view>
    <navigator url="/pages/house/form" class="btn bottom full">我要推荐</navigator>
  </view>