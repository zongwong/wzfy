
  <view class="container houseindex">
    <view class="index_list">
        <view bindtap="toDetail" class="index_item"  wx:for="{{list}}" wx:key="list*" data-wpytodetail-a="{{item.id}}">
            <image mode="aspectFill" src="{{item.thumb}}" class="post"/>
            <view class="title">
              <view>{{item.title}}</view>
              <view class="sub_title">{{item.description}}</view>
            </view>
        </view>
    </view>
    <navigator url="/pages/financial/form" class="btn bottom full">我要推荐</navigator>
  </view>